---
server_tag_name: "{{ hostvars['localhost']['server_tag_name'] }}"
server_ip:  "{{ ansible_default_ipv4.address }}"
server_name: "{{ inventory_hostname }}"
server_timezone: 'America/Chicago'
centos7_swap:
